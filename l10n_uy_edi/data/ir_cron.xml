<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data noupdate="1">

        <record id="ir_cron_get_ucfe_notif" model="ir.cron">
            <field name="name">Consult DGI State of Waiting CFE</field>
            <field name="interval_number">15</field>
            <field name="interval_type">minutes</field>
            <field name="numbercall">-1</field>
            <field name="model_id" ref="model_account_move"/>
            <field name="state">code</field>
            <field name="code">model.search([('l10n_uy_cfe_state', 'in', ['received'])]).action_l10n_uy_get_dgi_state()</field>
            <field name="active" eval="True"/>
        </record>

        <!-- TODO KZ generar el reporte diario en odoo a partir del generado en ucfe
            <field name="nextcall" eval="(DateTime.now().replace(hour=22, minute=0, second=0) + timedelta(days=1)).strftime('%Y-%m-%d %H:%M:%S')"/> -->

    </data>
</odoo>
