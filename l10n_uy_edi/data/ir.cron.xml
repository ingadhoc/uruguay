<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data noupdate="1">

        <!-- TODO test it first and then activate it -->
        <!-- <record id="ir_cron_get_ucfe_notif" model="ir.cron">
            <field name="name">Consult UCFE Notifications</field>
            <field name="interval_number">5</field>
            <field name="interval_type">minutes</field>
            <field name="numbercall">-1</field>
            <field name="model_id" ref="model_account_move"/>
            <field name="state">code</field>
            <field name="code">model.l10n_uy_get_ucfe_notif()</field>
            <field name="active" eval="True"/>
        </record> -->

        <!-- TODO generar el reporte diario en odoo a partir del generado en ucfe
            <field name="nextcall" eval="(DateTime.now().replace(hour=22, minute=0, second=0) + timedelta(days=1)).strftime('%Y-%m-%d %H:%M:%S')"/> -->

    </data>
</odoo>
