<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <data noupdate="1">

        <!-- <record id="ir_cron_consult_cfe" model="ir.cron">
            <field name="name">Consult CFE Sent to DGI</field>
            <field name="interval_number">1</field>
            <field name="interval_type">hours</field>
            <field name="numbercall">-1</field>
            <field name="model_id" ref="model_account_move"/>
            <field name="state">code</field>
            <field name="code">model.cron_send_dte_to_sii()</field>
            <field name="active" eval="True"/>
        </record>
        TODO hacer algo similar pero para consultar el estado en DGI
        -->

        <!-- TODO generar el reporte diario en odoo a partir del generado en ucfe
            <field name="nextcall" eval="(DateTime.now().replace(hour=22, minute=0, second=0) + timedelta(days=1)).strftime('%Y-%m-%d %H:%M:%S')"/> -->

    </data>
</odoo>
